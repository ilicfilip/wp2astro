{
  "name": "astro-wp-web-app",
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based CMS: WordPress Playground + GitHub + Cloudflare Pages",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@wp-playground/client": "^0.9.0",
    "blakejs": "^1.2.1",
    "octokit": "^4.0.0",
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.30.0",
    "vite": "^6.0.0",
    "wrangler": "^4.76.0"
  }
}
